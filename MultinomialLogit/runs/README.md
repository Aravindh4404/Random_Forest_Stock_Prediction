# runs/

Output folder for multinomial logit model runs.

## legacy

`legacy/` stores historical runs kept in git for comparison and traceability.

## local

`local/` stores new day-to-day runs.
This folder is git-ignored to avoid repository noise and large-file churn.
